// src/components/Items/OrderOverlay.jsx
// DEPRECATED: This component is no longer used. Customer details are now handled by CustomerDetailsModal.

import React from 'react';

const OrderOverlay = () => {
    // This component is disabled to prevent duplicate modals
    return null;
};

export default OrderOverlay;
